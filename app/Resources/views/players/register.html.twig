{% extends 'base.html.twig' %}
{% block body %}

{{ form_start(playerForm) }}
    {{ form_errors(playerForm.password.first) }}
    {{ form_widget(playerForm) }}
    <input type="submit" value="Register">
{{ form_end(playerForm) }}

{% endblock %}